
import 'package:flutter/material.dart';

class Model {
  String firstName;
  int weight;
  bool diabType;
  bool treatement;

  Model({this.firstName, this.weight, this.diabType, this.treatement});
}